# Supabase — set these in Vercel → Project → Environment Variables.
# The anon key is public by design; data is protected by Row Level Security (RLS).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
